services:
  - type: web
    name: shift-suite
    env: python
    region: singapore  # アジアに近いリージョン（日本から）
    plan: free  # 無料プランから開始（後でアップグレード可能）
    buildCommand: pip install --no-cache-dir --timeout=600 -r requirements.txt
    startCommand: gunicorn run_dash_server_production:server --bind 0.0.0.0:$PORT --workers 1 --timeout 120
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.8  # Downgraded from 3.13 - all 24 packages have cp312 wheels
      - key: DASH_ENV
        value: production
      - key: SESSION_TIMEOUT
        value: 3600
      # MAX_UPLOAD_SIZE削除 (Deploy 20.4): dash_app.pyのMAX_ZIP_SIZE_BYTES=10MBのみ使用
